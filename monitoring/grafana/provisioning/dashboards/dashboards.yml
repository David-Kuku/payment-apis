apiVersion: 1

# Load any dashboard JSON files found in the mounted dashboards folder.
providers:
  - name: payment-dashboards
    orgId: 1
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
